LevelUP.AI — Икигай: Архитектура продакшн-версииАнализ: голос + лицо + анкета → очередь → LLM → отчёт → Stripe paywallТекущий статус: всё в одном HTML, демо-данные, нет реального бэкенда1. Обзор системы2. СтекБраузер/PWA (Next.js)│ HTTPS▼API Gateway / Nginx│┌────┴────────────────┐│   Backend (Node.js) ││   REST + WebSocket  │└────┬────────────────┘│┌────┼──────────────────────────────┐│    │                              │▼    ▼                              ▼Postgres  Redis (Bull queue)     S3 / R2(данные)  (LLM jobs)             (аудио, фото)│LLM Worker(Claude API)
СлойТехнологияЗачемFrontendNext.js 14 (App Router)SSR для SEO лендинга, CSR для флоуBackendNode.js + FastifyБыстрый, типизируемый, хорошаяэкосистемаБДPostgreSQL 16 + Prisma ORMACID, JSONB для гибких report-данныхQueueBullMQ (Redis 7)Надёжная очередь с retry / dead-letterStorageCloudﬂare R2 (или AWS S3)Объектное хранилище для медиаPaymentsStripe (PaymentIntent + webhooks)Server-side, PCI compliantLLMAnthropic Claude APIАнализ транскрипта голоса + описания лицаAuthJWT (access 15min + refresh 7d)Stateless, refresh в httpOnly cookieDeployDocker Compose → Railway /RenderПростой старт, можно мигрировать в k8s3. Схема БД (Prisma)prisma
model User {  id          String    @id @default(cuid())  email       String    @unique  createdAt   DateTime  @default(now())  sessions    Session[]  analyses    Analysis[]  payments    Payment[]}model Session {  id           String    @id @default(cuid())  userId       String?                         // null = гость до оплаты  user         User?     @relation(fields: [userId], references: [id])  guestToken   String    @unique               // UUID для гостей  createdAt    DateTime  @default(now())  expiresAt    DateTime  analyses     Analysis[]}model Analysis {  id              String          @id @default(cuid())  sessionId       String  session         Session         @relation(fields: [sessionId], references: [i  userId          String?  user            User?           @relation(fields: [userId], references: [id])  // Медиа — ключи в R2/S3, не base64 в БД  audioKey        String?         // r2://bucket/audio/uuid.webm  photoKey        String?         // r2://bucket/photo/uuid.jpg  audioUrl        String?         // presigned, lifetime 1h  photoUrl        String?         // presigned, lifetime 1h
  // Анкета  ikigaiAnswers   Json?           // { love: [...], good_at: [...], ... }  // Очередь  jobId           String?  status          AnalysisStatus  @default(PENDING)  errorMessage    String?  retryCount      Int             @default(0)  // Результат  reportFree      Json?           // видно без оплаты  reportFull      Json?           // только после payment  reportVersion   Int             @default(1)  createdAt       DateTime        @default(now())  completedAt     DateTime?  payment         Payment?}enum AnalysisStatus {  PENDING       // создан, медиа ещё не загружены  QUEUED        // медиа загружены, job поставлен в очередь  PROCESSING    // worker взял job  DONE          // LLM ответил, результат сохранён  FAILED        // исчерпаны retry}model Payment {  id                String    @id @default(cuid())  analysisId        String    @unique  analysis          Analysis  @relation(fields: [analysisId], references: [id])
4. API — Маршруты4.1 Auth  userId            String?  user              User?     @relation(fields: [userId], references: [id])  stripePaymentIntentId  String  @unique  stripeCustomerId       String?  amount            Int           // в копейках/центах  currency          String        @default("usd")  status            PaymentStatus @default(PENDING)  createdAt         DateTime  @default(now())  paidAt            DateTime?}enum PaymentStatus {  PENDING  SUCCEEDED  FAILED  REFUNDED}POST /api/auth/guest            → { guestToken, sessionId }   // создатьсессию до регистрацииPOST /api/auth/register         → { accessToken } + set-cookie refreshPOST /api/auth/login            → { accessToken } + set-cookie refresh
4.2 Analysis ﬂowPOST /api/auth/refresh          → { accessToken }POST /api/auth/logout           → 200POST /api/analysesBody: { sessionId }→ { analysisId, audioUploadUrl, photoUploadUrl }// Создаём запись со статусом PENDING// Генерируем presigned PUT URLs прямо на R2 (загрузка без проксирования черезbackend)POST /api/analyses/:id/confirmBody: { ikigaiAnswers: { love: [...], good_at: [...], world_needs: [...],paid_for: [...] } }// Проверяем, что оба медиа-файла реально существуют в R2// Меняем статус → QUEUED// Добавляем job в BullMQ→ { status: "queued", jobId }GET  /api/analyses/:id/status→ { status, progress, jobId }// Polling или WebSocket — см. 4.4GET  /api/analyses/:id/report/freeAuth: sessionId или JWT→ { reportFree }   // только если status === DONEGET  /api/analyses/:id/report/fullAuth: JWT (paid user)→ { reportFull }   // только если payment.status === SUCCEEDED
4.3 Payments4.4 WebSocket / SSE5. Загрузка медиа — Presigned URL ﬂowPOST /api/payments/create-intentAuth: JWT или guestTokenBody: { analysisId }→ { clientSecret, paymentIntentId }// Создаём Stripe PaymentIntent на backend// Сохраняем в payment таблицу со статусом PENDING// Frontend получает clientSecret и вызывает stripe.confirmPayment()POST /api/webhooks/stripeHeaders: stripe-signature// Верифицируем подпись (STRIPE_WEBHOOK_SECRET)// Обрабатываем события://   payment_intent.succeeded → payment.status = SUCCEEDED, разблокируемreportFull//   payment_intent.payment_failed → payment.status = FAILED//   charge.refunded → payment.status = REFUNDED→ 200 (Stripe ждёт быстрый ответ)GET /api/analyses/:id/stream   // Server-Sent Events// Пока job в очереди — слать прогресс// data: { progress: 45, stage: "voice_analysis", log: "Анализируем темпречи..." }// data: { status: "DONE" }
Важно: медиа никогда не проходит через backend-сервер — только presigned URL → R2напрямую.6. LLM Worker (BullMQ)Frontend                Backend              R2/S3│                      │                   ││─ POST /analyses ────▶│                   ││                      │── createPresignedPut ──▶││◀──── { audioUploadUrl, photoUploadUrl } ──│   ││                      │                   ││─ PUT audioUploadUrl ─────────────────────────▶││─ PUT photoUploadUrl ─────────────────────────▶││                      │                   ││─ POST /analyses/:id/confirm ────────────▶│   ││                      │── headObject(key) ───▶││                      │◀─── 200 (exists) ─────││                      │── addJob(analysisId) │ ││◀──── { status: queued } ─────────────────│   │typescript
// workers/analysisWorker.tsconst worker =newWorker('analysis',async(job)=>{const{ analysisId }= job.data;// 1. Получить analysis из БДconst analysis =await prisma.analysis.findUnique({ where:{ id: analysisId }// 2. Установить статус PROCESSINGawait prisma.analysis.update({    where:{ id: analysisId },    data:{ status:'PROCESSING'}});// 3. Получить presigned GET URLs для медиаconst audioUrl =awaitgetPresignedGet(analysis.audioKey);const photoUrl =awaitgetPresignedGet(analysis.photoKey);// 4. Транскрибировать голос (Whisper API или Claude)const transcript =awaittranscribeAudio(audioUrl);// Whisper// 5. Описать фото (vision)const faceDescription =awaitdescribeFace(photoUrl);// Claude vision// 6. Позвать LLM для генерации отчётаconst prompt =buildIkigaiPrompt({    transcript,    faceDescription,    ikigaiAnswers: analysis.ikigaiAnswers,});const result =await anthropic.messages.create({
    model:'claude-opus-4-6',    max_tokens:4000,    system:IKIGAI_SYSTEM_PROMPT,    messages:[{ role:'user', content: prompt }],});const report =parseReport(result.content[0].text);// JSON из <report> тего// 7. Сохранить результат и обновить статусawait prisma.analysis.update({    where:{ id: analysisId },    data:{      status:'DONE',      reportFree: report.free,      reportFull: report.full,      completedAt:newDate(),}});// 8. Нотифицировать frontend через SSE/WSawaitemitProgress(analysisId,{ status:'DONE', progress:100});},{  connection: redis,  concurrency:3,// параллельно 3 job});// Retry-стратегияworker.on('failed',async(job, err)=>{if(job.attemptsMade>=3){await prisma.analysis.update({      where:{ id: job.data.analysisId},
Конфиг очереди:7. LLM Prompt-архитектура      data:{ status:'FAILED', errorMessage: err.message}});}});typescriptconst queue =newQueue('analysis',{  connection: redis,  defaultJobOptions:{    attempts:3,    backoff:{ type:'exponential', delay:5000},// 5s, 10s, 20s    removeOnComplete:100,// хранить последние 100 успешных    removeOnFail:500,// хранить последние 500 упавших}});typescript
constIKIGAI_SYSTEM_PROMPT=`Ты — эксперт-психоаналитик и карьерный стратег. Анализируешь профиль человека по:1. Транскрипту голосового фрагмента (темп, лексика, паттерны)2. Описанию внешнего вида и мимики на фото3. Ответам на анкету ИкигайВерни результат ТОЛЬКО в формате XML:<report>  <free>    {"profession": "...", "ikigai_scores": {...}, "summary": "..."}  </free>  <full>    {"voice_analysis": {...}, "face_analysis": {...},      "personality_metrics": [...], "top_roles": [...],      "career_action": "...", "final_insight": "..."}  </full></report>Не добавляй ничего вне тегов <report>.`;functionbuildIkigaiPrompt({ transcript, faceDescription, ikigaiAnswers }){return`ТРАНСКРИПТ ГОЛОСА:${transcript}ОПИСАНИЕ ФОТО:${faceDescription}ОТВЕТЫ НА АНКЕТУ ИКИГАЙ:- Что люблю делать: ${ikigaiAnswers.love?.join(', ')}- В чём хорош(а): ${ikigaiAnswers.good_at?.join(', ')}
8. Frontend — структура Next.jsЗагрузка медиа из браузера:- Что нужно миру: ${ikigaiAnswers.world_needs?.join(', ')}- За что могут платить: ${ikigaiAnswers.paid_for?.join(', ')}Сформируй полный Икигай-отчёт.`;}app/├── (marketing)/│   └── page.tsx               ← лендинг (SSR, SEO)├── (app)/│   ├── layout.tsx             ← PWA shell, nav│   ├── flow/│   │   ├── voice/page.tsx     ← запись голоса│   │   ├── face/page.tsx      ← загрузка фото│   │   ├── ikigai/page.tsx    ← анкета по зонам│   │   └── analysis/page.tsx  ← прогресс (SSE polling)│   ├── report/│   │   ├── [analysisId]/│   │   │   ├── free/page.tsx│   │   │   └── full/page.tsx  ← paywall или полный отчёт│   └── pay/[analysisId]/page.tsx  ← Stripe Elements└── api/└── ...                    ← Next.js route handlers (тонкий прокси)или отдельный backend
Прогресс через SSE:typescript// hooks/useMediaUpload.tsasyncfunctionuploadMedia(analysisId:string, audioBlob:Blob, photoBlob:Blob// 1. Получить presigned URLsconst{ audioUploadUrl, photoUploadUrl }=await api.createAnalysis(sessionId// 2. Загрузить напрямую в R2awaitPromise.all([fetch(audioUploadUrl,{ method:'PUT', body: audioBlob,      headers:{'Content-Type': audioBlob.type}}),fetch(photoUploadUrl,{ method:'PUT', body: photoBlob,      headers:{'Content-Type': photoBlob.type}}),]);// 3. Подтвердить и запустить анализawait api.confirmAnalysis(analysisId, ikigaiAnswers);}typescript
9. Платёжный флоу (Stripe)// hooks/useAnalysisProgress.tsfunctionuseAnalysisProgress(analysisId:string){const[state, setState]=useState({ status:'QUEUED', progress:0});useEffect(()=>{const es =newEventSource(`/api/analyses/${analysisId}/stream`);    es.onmessage=(e)=>{const data =JSON.parse(e.data);setState(data);if(data.status==='DONE'|| data.status==='FAILED') es.close();};return()=> es.close();},[analysisId]);return state;}Frontend                    Backend                Stripe│                           │                     ││── POST /payments/         │                     ││   create-intent ─────────▶│                     ││                           │── createPaymentIntent ──▶││                           │◀── { clientSecret } ────││◀── { clientSecret } ──────│                     ││                           │                     ││── stripe.confirmPayment() │                     ││   (Stripe.js на клиенте) ─────────────────────▶│
Backend — создание PaymentIntent:│◀── redirect / success ────────────────────────││                           │                     ││                           │◀── webhook: payment_intent.succeeded│                           │                     ││                           │── UPDATE payment.status = SUCCEEDED│                           │── unlock reportFull│                           │── 200 ──────────────▶││                           │                     ││── GET /report/full ──────▶│                     ││◀── { reportFull } ────────│                     │typescript
Backend — webhook:app.post('/api/payments/create-intent', authenticate,async(req, res)=>{const{ analysisId }= req.body;// Проверяем что analysis существует и принадлежит юзеруconst analysis =await prisma.analysis.findFirst({    where:{ id: analysisId, sessionId: req.session.id}});if(!analysis || analysis.status!=='DONE'){return res.status(400).json({ error:'Analysis not ready'});}const paymentIntent =await stripe.paymentIntents.create({    amount:990,// $9.90    currency:'usd',    metadata:{ analysisId, sessionId: req.session.id},});await prisma.payment.create({    data:{      analysisId,      stripePaymentIntentId: paymentIntent.id,      amount:990,      currency:'usd',}});  res.json({ clientSecret: paymentIntent.client_secret});});
typescript
app.post('/api/webhooks/stripe',  express.raw({ type:'application/json'}),// raw body для верификацииasync(req, res)=>{const sig = req.headers['stripe-signature'];let event;try{      event = stripe.webhooks.constructEvent(req.body, sig, process.env.STRIPE_W}catch(err){return res.status(400).send(`Webhook Error: ${err.message}`);}switch(event.type){case'payment_intent.succeeded':{const pi = event.data.object;await prisma.payment.update({          where:{ stripePaymentIntentId: pi.id},          data:{ status:'SUCCEEDED', paidAt:newDate()}});break;}case'payment_intent.payment_failed':{const pi = event.data.object;await prisma.payment.update({          where:{ stripePaymentIntentId: pi.id},          data:{ status:'FAILED'}});break;}}    res.json({ received:true});// Stripe ждёт 200 в течение 30 сек
10. Обработка ошибок — матрицаСценарийПоведениеR2 upload timeoutFrontend показывает retry; presigned URL живёт 15 минJob упал 1-2 разаBullMQ exponential backoﬀ, юзер видит «ещё обрабатывается»Job упал 3 разаstatus = FAILED, показываем «Попробовать снова» (бесплатно)LLM rate limitRetry с backoﬀ; при сохраняющейся ошибке — уведомление наemailClaude вернул невалидныйJSONparseReport с fallback + логирование в SentryStripe webhook не дошёлStripe ретраит 72 часа; можно проверить вручную поPaymentIntent IDДубль webhookIdempotency: проверяем payment.status !== PENDINGперед обновлениемЮзер закрыл браузерSSE переподключается при открытии; статус в БД11. Переменные окружения});
12. Docker Compose (dev)yaml
version:'3.9'services:postgres:image: postgres:16-alpineenvironment:POSTGRES_DB: levelupPOSTGRES_USER: levelupPOSTGRES_PASSWORD: dev_passwordvolumes:- pg_data:/var/lib/postgresql/dataports:-"5432:5432"redis:image: redis:7-alpineports:-"6379:6379"api:build: ./backendenvironment:DATABASE_URL: postgresql://levelup:dev_password@postgres:5432/levelupREDIS_URL: redis://redis:6379depends_on:[postgres, redis]ports:-"3001:3001"volumes:- ./backend:/appworker:build: ./backendcommand: node dist/workers/analysisWorker.js
13. Порядок разработки (спринты)Спринт 1 — Скелет (1–2 недели) Репозиторий: monorepo (turborepo) apps/frontend, apps/backend Docker Compose: Postgres + Redis + backend + frontend Prisma schema + первая миграция Auth: register/login/refresh + middlewarePOST /analyses + presigned R2 URLsPOST /analyses/:id/confirm + BullMQ job Worker с заглушкой (sleep 5s → DONE с fake reportFull) SSE endpoint с прогрессомenvironment:DATABASE_URL: postgresql://levelup:dev_password@postgres:5432/levelupREDIS_URL: redis://redis:6379depends_on:[postgres, redis, api]frontend:build: ./frontendports:-"3000:3000"environment:NEXT_PUBLIC_API_URL: http://localhost:3001volumes:  pg_data:
Спринт 2 — Реальный LLM (1–2 недели) Whisper: транскрипция аудио из R2 Claude vision: описание фото Полный prompt + парсинг XML-ответа Сохранение reportFree / reportFull в БД Retry-стратегия + SentryСпринт 3 — Оплата (1 неделя) Stripe PaymentIntent на backend Stripe Elements на frontend Webhook: payment_intent.succeeded → unlock reportFull Тест в Stripe test-modeСпринт 4 — Frontend флоу (2 недели) Миграция экранов из HTML: voice → face → ikigai → analysis → report Presigned upload из браузера SSE progress-screen Free report + paywall + full report Stripe Elements UIСпринт 5 — Полировка (1 неделя) Лендинг (Next.js SSR) Email после завершения анализа (Resend / Postmark) Rate limiting (fastify-rate-limit) CORS, Helmet, input validation (Zod) Логирование (Pino) + мониторинг (Sentry) Деплой на Railway / Render
14. Что из текущего HTML переиспользоватьКомпонентСтатусCSS-дизайн-система (переменные, карточки,кнопки)✅ Перенести в Tailwind-конфиг или CSSmodulesIkigai SVG-карта✅ Вынести в React-компонентЭкраны voice/face/ikigai/analysis/report✅ Портировать JSX по одномуanalyzeFaceImage (pixel math)⚠ Оставить как pre-check на клиенте, реальныйанализ — на бэкендеgenerateReport (статические данные)❌ Полностью заменить на API callPaywall-логика❌ Заменить на Stripe + webhookВсе прямые вызовы AI❌ Убрать, перенести в worker